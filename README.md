# HCC Crosswalk Tool

A React web application for mapping ICD-10 codes to HCC codes.

## Features
- Search ICD-10 codes
- View HCC mappings
- Export to CSV
- Multiple CMS-HCC model support

## Deployment
This app is configured for GitHub Pages deployment.

Run `npm run deploy` to deploy.
